node_modules
.next
.env.local
*.log

# TypeScript's incremental build cache. One is written per project file
# (tsconfig.tsbuildinfo, tsconfig.build.tsbuildinfo), it changes on every
# typecheck, and it is machine-specific — so it is noise in every diff.
*.tsbuildinfo
